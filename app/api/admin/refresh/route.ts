import {NextRequest,NextResponse} from 'next/server';import {marketDataProvider} from '@/lib/providers';export async function POST(req:NextRequest){const auth=req.headers.get('authorization');if(!auth||auth!==`Bearer ${process.env.ADMIN_TOKEN}`)return NextResponse.json({error:'Unauthorized'},{status:401});try{const status=await marketDataProvider.getMarketStatus();return NextResponse.json({ok:true,status,message:'Provider refresh hook is ready. Configure the provider adapter to persist quotes.'});}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:'Provider error'},{status:503})}}
